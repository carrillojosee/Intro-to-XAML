<Page
    x:Class="RestaurantManager.ExpediatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:RestaurantManager.Models"
    mc:Ignorable="d">
    <Page.DataContext>
        <data:DataManager/>
    </Page.DataContext>

    <Grid Background="Black"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="6*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <AppBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" IsOpen="True" >
            <AppBarButton  Icon="Home" Click="HomeButtonEP" />
        </AppBar>

        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="0" Margin="0,20,0,0">
            <GridView Name="ExpediatePageGV" ItemsSource="{Binding OrderItems}" />
        </StackPanel>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1">
            <Button Foreground="White" Content="Clear All Orders" Click="Button_Click" />
        </StackPanel>
        
    </Grid>
</Page>
